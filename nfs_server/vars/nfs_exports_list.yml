---
nfs_exports:
  - /ravi:
      export_options:
        - ubuntu1(ro,sync)
        - centos2(rw,sync)
        - centos4(rw,sync,no_root_squash)
  - /videos:
      export_options:
        - centos4(rw,sync,no_root_squash)
        - ubuntu1(ro,sync)
